{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next",
      "config": {
        // Use the main buildCommand below instead of configuring here
      }
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "prisma generate && next build",
  "regions": ["iad1"] 
} 