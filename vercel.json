{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next",
      "config": {
        // Use the main buildCommand below instead of configuring here
      }
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "(rm -f app/api/listings/[listingId]/page.tsx app/api/listings/[listingId]/page.js app/api/listings/[listingId]/page.jsx || true) && npm run build",
  "regions": ["iad1"] 
} 